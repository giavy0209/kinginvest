<%components.forEach(com =>{%>
    <%
    var getData = (key) => {
        var comData = com.value.datas.find(o => o.key === key)
        if(comData){
            return comData.data
        }
        return null
    }
    %>
    <%-include ('./components/' + com.value.key + '/index', {data : com.value, getData : getData})%>    
<%})%>